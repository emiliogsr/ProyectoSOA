package com.example.business;

import org.springframework.stereotype.Service;

import com.example.common.AppException;
import com.example.dto.Request;
import com.example.dto.Response;

/**
 * Calculadora aritmmética simple.
 */
@Service
public class ValidadorUsuarios{

	/**
	 * Realiza una operación aritmética básica entre dos números.
	 * 
	 * @param request La solicitud que contiene los números y la operación.
	 * @return La respuesta que contiene el resultado de la operación.
	 */
//logica
	
}